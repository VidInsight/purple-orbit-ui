services:
  # Kapıcı (Reverse Proxy) - Hiç kapanmaz, trafiği yönetir
  gateway:
    image: nginx:alpine
    container_name: ui-gateway
    ports:
      - "80:80"
    volumes:
      - ./gateway.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - purple-orbit-ui
    networks:
      - app-network

  # Uygulama
  purple-orbit-ui:
    image: ghcr.io/${{ github.repository }}:latest
    restart: always
    deploy:
      resources:
        limits:
          cpus: '0.50'
          memory: 512M
    healthcheck:
      test: [ "CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:8080/" ]
      interval: 5s
      timeout: 3s
      retries: 5
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
